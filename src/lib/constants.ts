/**
 * Shared application constants.
 *
 * Centralises policy values so that every validation layer (frontend, API, Zod schemas)
 * stays in sync from a single source of truth.
 */

/** Minimum number of characters required for any user password. */
export const MIN_PASSWORD_LENGTH = 8
